# insight-list: 멘트리 인사이트 — 감사 기록

- 감사일: 2026-09-16
- 대상: [`template/`](template/) **9본** — 얇은 페이지 8장 ＋ 공용 화면 부품 `InsightListScreen`
- 디자인 시스템: 감사는 `ds-export/` **43본**(SHA-256 `7649857370a5`)으로 했고, 같은 날 16:38 에 `ds-export/` 가 export 와 같은 판(폴더 해시 `4ec9a3f66119`)으로 교체됐다. 교체 뒤 재렌더는 아래 「DS 교체 뒤」
- 방법: export 20장을 1280 으로 전부 렌더 → 리포 경로에서 **1280 · 1024 · 768 · 375** 재렌더 → **iframe 프로브로 눌러서 확인**

## 375px 는 iframe 으로 쟀다

**headless Chrome 은 창 최소 폭이 500px 다.** 375 폭 `iframe` 을 420 창 안에 넣어 진짜 375 뷰포트를 만들었다. 스크롤바가 있어 콘텐츠 폭은 360 이다.

## 눌러서 확인했다

**「부품이 그린다」로 넘기지 않았다.** 프로브가 실제로 눌렀다. JS 오류 0건.

| 조작 | 결과 | 판정 |
|---|---|---|
| 카드 썸네일의 스크랩 토글 | `aria-pressed` false → true | **OK** |
| 「국가」 필터 → 「일본」 → 「1개 적용하기」 | 칩 「일본」 ＋ 「필터 초기화」가 생기고 카드 10장 → 5장 | **OK** |
| 검색창에 「비자」 ＋ Enter | 칩 「검색: 비자」 ＋ 카드 1장 | **OK** |
| 정렬 「오래된순」 | 첫 카드가 「2026년 일본 취업을…」 → 「사이보즈 백엔드 엔지니어…」 | **OK**(export 번들) |
| 페이지 「2」 | 현재 표시 1 → 2 | **OK.** 단 카드가 그대로다 — 스크립트가 늘 앞 10장을 자른다. 표본이 12장이라 2장이 보여야 맞다 → [insight-fix](../../_import/prompts/insight-fix.md) 2 |
| 카테고리 4항목 | `insight-list-all` · `-insight` · `-event` · `-interview` 로 | **OK** |
| 카드 10장의 링크 | 인사이트 6 → `article-detail-insight` · 이벤트 3 → `-event` · 인터뷰 1 → `-interview` | **OK.** 카테고리에 맞는 상세로 간다 |
| 375 의 카테고리 | `Tabs` 4개 | **OK** |

**정렬은 번들에 따라 다르게 그려졌다.** 감사 때 리포의 43본 번들에서는 네이티브 `<select>` 였고, 교체 뒤에는 export 와 같은 커스텀 리스트박스다. 둘 다 반응한다.

## 6품질축

| 축 | 결과 |
|---|---|
| 레이아웃 | **OK.** 좌 240 카테고리 ＋ 우 본문. 조건 줄이 카테고리 4형대로 바뀐다(전체 · 인사이트 ＝ 국가 · 키워드 · 검색 ／ 이벤트 ＝ 종류 · 검색 ／ 인터뷰 ＝ 국가 · 직종 · 키워드 · 검색). 가로형 카드 ＋ 항목 사이 1px 선. 가로 넘침 **1280 · 1024 · 768 · 375 전부 0** |
| 컬러 | **OK.** 태그는 부품대로 컬러(indigo · amber · 2026-09-16 결정). 선택 카테고리는 옅은 초록 채움. 그 밖에 새 색 없음 |
| 타이포그래피 | **OK.** 제목 h0 · 카드 제목 2줄 클램프 · 발췌 3줄 클램프 · 발행일 caption |
| 인터랙션 | **OK.** 위 표. 컬러 태그는 눌리지 않는다(표시 전용 — 사양대로) |
| 접근성 | 아래 |
| 반응형 | **OK.** 375 에서 카테고리 → `Tabs`, 카드 → `ArticlePreview` 세로형, 조건은 국가 · 키워드 2단 ＋ 검색 한 줄. 768 문서 폭 753 · 1024 → 1009 · 1280 → 1265(스크롤바 15 제외) |

## 접근성 — 사람이 판정하는 항목

| 확인 | 결과 |
|---|---|
| 이미지 `alt` | img 21 중 20 이 `alt=""`(장식). 썸네일의 이름은 카드 제목이 준다 → **OK** |
| 색만으로 전달 | 선택 카테고리는 채움 ＋ `aria-current="page"` → **OK** |
| 포커스 순서 | 카테고리 → 조건 → 칩 → 목록 → 페이지. 시각 순서와 같다 → **OK** |
| 제목 레벨 | `h1` 하나뿐이다. **임시 조립 카드의 제목이 heading 이 아니다**(`ArticlePreview` 부품은 h3 를 쓴다) → **미달 기록.** `DS-33` 부품으로 갈아끼우면 풀린다 |
| 폼 라벨 | **검색창이 `placeholder` 만 갖는다.** 정렬(43본 번들에서 네이티브 `<select>`)도 라벨이 없다 → **3.3.2 미달 기록** → insight-fix 5 |
| `lang` · 페이지 제목 | **얇은 페이지 16장 전부 `<html lang>` 과 `<title>` 이 없다** → **3.1.1 · 2.4.2 미달 기록** → insight-fix 4 |

**대비 · 포커스 표시 · 타겟 사이즈는 기계 검사가 없다.** 눈으로 본 것 — 스크랩 토글 38px(`BookmarkToggle` 부품 그대로). 문제를 발견하지 못했다. 안 본 것이 아니라 못 찾은 것이다.

## 부품이 못 따라가는 것 — 화면에서 우회하지 않았다

| 무엇 | 지금 template | 어디로 |
|---|---|---|
| 카테고리 세로 내비 | **임시 조립** `data-temp="DS-17"` — `Card` ＋ 항목 4 | `DS-17` 이 오면 그 자리만 바꾼다(상혁 2026-09-16) |
| 가로형 아티클 카드 | **임시 조립** `data-temp="DS-33"` | `DS-33` |
| 페이지 이동 시작형 | `1 2 3 … 30` 으로 뜬다. 와이어는 `1~7 … 30` | `DS-27` 요구 중 |
| 정렬 셀렉트 | 43본 번들은 네이티브 `<select>` | DS 재export 로 풀린다 |
| 아이콘 | DS 의 `Icon` 이 HugeIcons 를 CDN 에서 mask 로 읽는다. 오프라인이면 빈다. 375 스크린샷 하나가 그렇게 찍혔다 | 디자인 시스템의 판단. 기표 후보 |

## 폰트

`Pretendard JP Variable` 은 리포에 없다. 대체 폰트로 그려진다. 클램프 줄 수는 같아도 줄바꿈 자리가 실제와 다를 수 있다.

## 다음 export 때 고칠 것

[insight-fix.md](../../_import/prompts/insight-fix.md) — 페이지별 카드 자르기(2) · `lang` · `title`(4) · 검색창 · 정렬의 접근 이름(5). 급하지 않다.

## DS 교체 뒤 — 2026-09-16 16:38

같은 날 디자이너가 `M2 Design System` 을 재export 해 `ds-export/` 를 교체했다(폴더 해시 `4ec9a3f66119`). 이 template 은 그 번들로 그려지므로 다시 렌더했다.

| 페이지 | 1280 | 375 | 깨진 이미지 |
|---|---|---|---|
| `insight-list-all` | 1265 | 360 | 0 / 21 |

**가로 넘침 0 · 깨진 이미지 0.** 교체로 바뀐 것은 부품 안이다 — `Header` 가 3열 그리드로 서고, 정렬 `Select` 가 커스텀 리스트박스가 됐고, 글자마다 line-height · letter-spacing 토큰이 붙었다. 화면 파일은 건드리지 않았다.

