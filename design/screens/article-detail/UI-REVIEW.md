# article-detail: 아티클 상세 — 감사 기록

- 감사일: 2026-09-16
- 대상: [`template/`](template/) **9본** — 얇은 페이지 8장 ＋ 공용 화면 부품 `ArticleDetailScreen`
- 디자인 시스템: 감사는 `ds-export/` **43본**(SHA-256 `7649857370a5`)으로 했고, 같은 날 16:38 에 `ds-export/` 가 export 와 같은 판(폴더 해시 `4ec9a3f66119`)으로 교체됐다. 교체 뒤 재렌더는 아래 「DS 교체 뒤」
- 방법: export 20장을 1280 으로 전부 렌더 → 리포 경로에서 **1280 · 1024 · 768 · 375** 재렌더 → **iframe 프로브로 눌러서 확인**

## 375px 는 iframe 으로 쟀다

[insight-list 의 감사 기록](../insight-list/UI-REVIEW.md)과 같다. 콘텐츠 폭은 360 이다.

## 결함 2건 — 배치 때 고쳤다

**둘 다 참여 멘토 4장(이벤트 기사 A형)이다.** 스타일 조정 범위라 배치 스크립트가 고쳤고 원본 고침은 [insight-round2](../../_import/prompts/insight-round2.md) 1 이다.

| # | 무엇 | 실측 | 원인 | 고친 것 |
|---|---|---|---|---|
| 1 | **375 에서 문서 폭 1400** | 카드 4장이 한 줄 | `.qi-mentors` 의 인라인 `display:flex` 가 미디어 규칙의 `display:grid` 를 이긴다 | 미디어 규칙에 `!important`. 그래도 **2열은 31px 넘친다** — `MentorCard` 모바일 최소 폭 179 × 2 ＋ 16 ＞ 343(`DS-37`). **1열**로 뒀다. 사양 §4 · §8 정정 |
| 2 | **1280 에서 15px · 1024 에서 271px 넘침** | `MentorCard` 296 고정 × 4 ＋ 간격 72 ＝ 1256 ＞ 1232 · 976 | 맨줄 flex 에 세웠다. DS 는 멘토 카드 줄을 `Carousel`(peek)로 정한다 | `overflow-x:auto` 로 가로 스크롤 줄. 사양 §6 을 **`Carousel`** 로 바꿨다 |

고친 뒤 재측정 — **1280 → 1265 · 1024 → 1009 · 768 → 753 · 375 → 360.** 전부 뷰포트 안이다. 다른 7장은 처음부터 넘침 0 이었다.

## 눌러서 확인했다

| 조작 | 결과 | 판정 |
|---|---|---|
| 레일의 스크랩 | `aria-pressed` false → true. variant outline → secondary | **OK**(1280 · 375) |
| 레일의 공유 | `Toast` 「링크를 복사했어요」 | **OK** |
| 스크롤 75% | 구독 카드 등장 「새로운 아티클 발행 소식을 받아보시겠어요?」 | **OK.** 처음엔 DOM 에 없다가 70% 를 지나면 생긴다 |
| 구독 카드 ✕ | 사라진다. `sessionStorage` 에 남겨 이 세션에 다시 안 뜬다 | **OK** |
| 「멘트리 아티클 구독하기」 | 카드 사라짐 ＋ `Toast` 「새 아티클 소식을 메일로 보내드릴게요」 | **OK** |
| 배너 B 「관련 멘토 보기」 | `article-detail-modal-mentors` 로. 모달은 `Dialog` ＋ `MentorCard` 3 ＋ 「멘토 찾기에서 더 보기」. 닫으면 `article-detail-insight` 로 | **OK** |
| ← · 브레드크럼 「멘트리 인사이트」 | `insight-list-all` 로 | **OK** |
| 관련 아티클 4장 | `article-detail-insight` 로 | **OK** |
| 목차 항목 · 스크롤 따라오기 | **헤드리스에서 검증하지 못했다.** 스크롤스파이가 `requestAnimationFrame` 으로 재는데 가상 시간에서 rAF 가 돌지 않는다. **코드로 확인** — 뷰포트 위 1/3 선을 지난 마지막 H2 가 현재. H2 가 없는 `plain` 판에서는 목차를 세우지 않는다(렌더 확인) | 보류 — 브라우저에서 사람이 한 번 본다 |

**배너 A 「참여 신청하기」와 C 「멘토링 신청하기」, 멘토 카드는 `href="#"` 다.** 이벤트 신청 화면과 멘토 상세는 이번 범위 밖이다(사양 §9).

## 6품질축

| 축 | 결과 |
|---|---|
| 레이아웃 | **OK(고친 뒤).** 브레드크럼 → 가운데 머리 → 3단(레일 48 · 본문 720 · 목차 200) → 배너 → 부속 블록. A · B · C 판이 사양 §2 표대로 갈린다. `plain` 은 목차 · 부속 · 구독 카드가 없다 |
| 컬러 | **OK.** 태그 컬러(목록과 같다). 형광 강조 `--sage-100` 계열. 배너 옅은 초록 ＋ `--primary` 테두리. 목차 현재는 초록 선 ＋ 초록 글자 |
| 타이포그래피 | **OK.** 제목 display · H2 3개 · 본문 줄 간격 1.8 · 캡션 caption |
| 인터랙션 | **OK.** 위 표. 숫자는 어디에도 없다(주석 「숫자 비표시」) |
| 접근성 | 아래 |
| 반응형 | **OK(고친 뒤).** 375 에서 레일은 발행일 아래 가운데 한 줄, 목차 숨김, 배너 세로 쌓임, 관련 아티클 1열, 참여 멘토 1열, 구독 카드 탭바 위 좌우 꽉 |

## 접근성 — 사람이 판정하는 항목

| 확인 | 결과 |
|---|---|
| 이미지 `alt` | 본문 이미지와 카드 썸네일은 `alt=""`. 본문 이미지는 캡션이 설명한다 → **OK** |
| 색만으로 전달 | 레일 토글은 채움 ＋ `aria-pressed`. 목차 현재는 선 ＋ 글자 ＋ `aria-current` → **OK** |
| 포커스 순서 | ← → 머리 → 레일 → 본문 → 목차 → 배너 → 부속. 레일이 본문 앞이다 — 시각 위치(왼쪽)와 같다 → **OK** |
| 제목 레벨 | `h1` → `h2` × 4(본문 3 ＋ 부속 제목 1). 건너뛰지 않는다 → **OK** |
| 폼 요소 | 없다 → 해당 없음 |
| `lang` · 페이지 제목 | **없다** → **3.1.1 · 2.4.2 미달 기록** → insight-round2 6 |
| 이름 없는 버튼 · 링크 | 0 · 0 → **OK** |

**대비 · 포커스 표시 · 타겟 사이즈는 기계 검사가 없다.** 눈으로 본 것 — 레일 `IconButton` 48px. 문제를 발견하지 못했다.

## 부품이 못 따라가는 것 — 화면에서 우회하지 않았다

| 무엇 | 지금 template | 어디로 |
|---|---|---|
| 좌측 고정 레일(숫자 없는 토글) | **임시 조립** `data-temp="rail"` — `IconButton` variant 를 outline↔secondary 로 | `DS-35` |
| 우측 목차 | **임시 조립** `data-temp="toc"` | `DS-17` 앵커 변형 |
| 구독 카드 | **임시 조립** `data-temp="subscribe"` — `Card` ＋ `Button` ＋ ✕ | `DS-36` |
| 배너(문구 ＋ 버튼) | 조립. `qna-detail` 과 2화면째 | `DS-34` |
| 참여 멘토 4장 | 가로 스크롤 줄(배치 때) | 사양은 `Carousel`. 모바일 2열은 `DS-37` |
| 아이콘 | CDN mask. 375 스크린샷 한 장에서 레일 · ← 아이콘이 비어 찍혔다 — 재촬영에서는 그려졌다. 오프라인이면 빈다 | 디자인 시스템의 판단. 기표 후보 |

## 폰트

[insight-list 의 감사 기록](../insight-list/UI-REVIEW.md)과 같다.

## 다음 export 때 고칠 것

[insight-round2.md](../../_import/prompts/insight-round2.md) — 참여 멘토 · 관련 아티클 `Carousel`(1 · 2) · 「목록으로 돌아가기」(3) · 임시 문구 표시(5) · `lang` · `title`(6). 급하지 않다.

## DS 교체 뒤 — 2026-09-16 16:38

같은 날 디자이너가 `M2 Design System` 을 재export 해 `ds-export/` 를 교체했다(폴더 해시 `4ec9a3f66119`). 이 template 은 그 번들로 그려지므로 다시 렌더했다.

| 페이지 | 1280 | 375 | 깨진 이미지 |
|---|---|---|---|
| `article-detail-insight` | 1265 | 360 | 0 / 6 |

**가로 넘침 0 · 깨진 이미지 0.** 교체로 바뀐 것은 부품 안이다 — `Header` 가 3열 그리드로 서고, 정렬 `Select` 가 커스텀 리스트박스가 됐고, 글자마다 line-height · letter-spacing 토큰이 붙었다. 화면 파일은 건드리지 않았다.

