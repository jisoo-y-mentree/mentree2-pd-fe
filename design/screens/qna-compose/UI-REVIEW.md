# qna-compose: Q&A 질문 작성 — 감사 기록

- 감사일: 2026-09-14 · **재감사 2026-09-15**(Figma 대조판)
- 대상: [`template/`](template/) **6장**
- 디자인 시스템: `ds-export/` **43본**(SHA-256 `7649857370a5`)
- 방법: headless Chrome 으로 **1280 · 768 · 375** 3폭 렌더 ＋ Figma 와이어 대조

## 375px 는 iframe 으로 쟀다

**headless Chrome 은 창 최소 폭이 500px 다.** `--window-size=375` 를 주면 레이아웃은 500 인 채 **스크린샷만 잘려** 가짜 넘침이 된다.

375 폭 `iframe` 을 420 창 안에 넣어 **진짜 375 뷰포트**를 만들어 다시 쟀다. [design-bolt](../../../.claude/skills/design-bolt/SKILL.md) 가 경고한 함정이다.

## 6품질축

| 축 | 결과 |
|---|---|
| 레이아웃 | **0건** |
| 컬러 | **0건** — 의미색이 일관된다. 채택은 green, 도움돼요는 rose |
| 타이포그래피 | **1건** — 아래 |
| 인터랙션 | **해당 없음.** `template/` 은 정적이라 hover·focus 를 재현하지 않는다. 의도는 UI-SPEC §8 이 갖는다 |
| 접근성 | 아래 |
| 반응형 | **0건** — 1280 · 768 · 375 에서 가로 넘침·요소 붕괴가 없다 |

**타이포그래피 0건** — 이 화면에는 섹션 제목이 없어 `DS-29` 가 안 걸린다.


## 접근성 — 사람이 판정하는 항목

| 확인할 것 | 결과 |
|---|---|
| 이미지·아이콘의 `alt` | **미확인.** 부품이 그리므로 화면에서 판정할 수 없다 |
| 색만으로 정보를 전달하지 않는다 | **OK** |
| 포커스 순서가 시각 배열과 맞는다 | **미확인.** 정적 파일이라 탭 순서를 볼 수 없다 |
| 제목 레벨이 건너뛰지 않는다 | **OK** — `h1` 하나에 `Field` 라벨이 따른다 |
| 폼 요소에 `label` | **OK** — 5칸 전부 `Field` 로 감싸 라벨이 붙는다. 플레이스홀더로 대신하지 않는다 |
| `lang` 과 페이지 제목 | **`lang="ko"` 있다.** 페이지 제목은 부품 밖이라 화면이 정한다 |

**대비·포커스·타겟 사이즈는 화면에서 고치지 않는다.** 미달을 찾으면 `DS-nn` 으로 기표한다([designer-workflow](../../../docs/guidelines/designer-workflow.md)).

## 부품이 못 따라가는 것 — 화면에서 우회하지 않았다

**없다.** 이 화면이 기다리는 부품이 없다.

**본문 에디터는 자리만 그렸다.** `RichTextEditor` 는 각하됐고(`DS-15`) FE 가 blocknote 를 붙인다. 툴바와 글자 수 카운터의 위치만 보인다.


**전부 그대로 두었다.** 부품 밖에서 비슷하게 그리면 나중에 두 벌이 된다. 디자인 시스템 개정은 **화면이 다 나오고 흐름이 보일 때 한 번에** 한다(2026-09-14 결정).

## 폰트

**대체 폰트로 그려진다.** `Pretendard JP Variable` 의 바이너리를 리포에 넣지 않는다. **자간과 줄바꿈이 실제와 다르게 보일 수 있다** — 이 감사는 배치와 구조를 본 것이지 조판을 확정한 것이 아니다.

## 2026-09-15 재감사 — Figma 대조판

****6장** 전부를 1280 폭 · 세로 전체로 렌더해 봤다.** 렌더된 DOM 의 문자와 페이지 스크립트의 상태 배선도 같이 봤다.

| 확인 | 결과 |
|---|---|
| 2026-09-15 결정 8건이 들어갔는가 | **OK** — 질문 블록 맨바닥 · 정렬 없음 · 임시 저장 없음 · 이탈 모달 프레임 문구 · 멘토 안내 띠 ＋ 답변 작성 카드 · 검색 0건에 우측 없음 · 감사인사 대화상자 · 되돌아가기 ← |
| 부품 인터랙션이 살아 있는가 | **OK** — `QnaCard`·`CountToggle`·`Tabs`·`ToggleGroup`·`Select`·`SearchInput`·`Pagination` 에 상태와 `onChange` 를 넘긴다. 도움돼요를 누르면 숫자가 ±1 되고, 답변 0건 카드를 스크랩하면 `Toast` 가 뜬다 |
| `Field` 의 필수 표시·설명이 부품 것인가 | **OK** — `required`·`description` 을 넘긴다. 배지를 그리지 않았다 |
| 화면 상태를 갈아끼우는 JS 가 없는가 | **OK** — 파일마다 화면 상태 하나 |
| 링크 대상 파일이 전부 있는가 | **OK** — 배치 스크립트가 검사했다 |
| 부품 밖 손그림 | **변화 없음** — CTA 박스(`DS-31`) · 스켈레톤 프레임 · 멘토 플로팅 버튼은 부품 그대로. 와이어의 플로팅 카드는 부품이 없어 `DS-32` 후보로 남긴다 |

