# 의존
node_modules/
.pnpm-store/

# 빌드 산출물
storybook-static/
.next/
dist/
build/
*.tsbuildinfo

# 브라우저(playwright-core 가 받는 것)
.cache/
ms-playwright/

# 환경
.env
.env.*
!.env.example

# 개인 설정(팀 공유는 .claude/settings.json)
.claude/settings.local.json

# Claude Design 「경위」의 반입 자리 — 대화·재개 팩·이관 메모(docs/SOURCES.md 「반입 방법」).
# 「사양」(토큰·컴포넌트·원칙)은 design/ds-export/ 에 두고 커밋한다. 이쪽은 무시하지 않는다.
design/_import/

# OS·에디터
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea/
